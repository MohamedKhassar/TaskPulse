/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.pngall.com",
      "www.google.com",
      "images.unsplash.com",
      "i.imgur.com",
    ],
  },
};

export default nextConfig;
